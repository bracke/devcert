# Coding Style

The repository follows the Ada Style Guide where mechanically checkable.

The Ada tooling enforces:

* spaces only
* maximum line length of 100 characters
* lower-case file names
* trailing whitespace rejection
* dependency boundary checks through manifests
* Ada-only repository tooling

Project tooling belongs in `devcert_tools`.
