# Localization

Human-readable terminal text is localized through `i18n`. The default locale is
`en`, stored at `config/messages/en.catalog`.

Required message identifiers include:

* `app.name`
* `cli.usage`
* `error.unknown_command`
* `json.schema`
* `release.passed`
